# assets/img/

Add your photo here as **`profile.jpg`** (square photo, at least 400×400px
works best — it's cropped into a circle).

The hero currently shows a gradient "PB" placeholder because this file
doesn't exist yet. As soon as you add `assets/img/profile.jpg`, it will
replace the placeholder automatically — no code changes needed.
